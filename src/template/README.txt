"create" is initiated manually once per geographic area (country, region or locality). It creates content.md. Target is sites/
"build" is used automatically for every build. It creates data.yml. Target is pages/
"locni" is also used for every build and automates aspects of building locnis and directory tables. It creates content.md, data.yml, and other files.

